# isaaclab 코어(source/isaaclab) 설치용 pip constraints
#
# 왜 필요한가:
#   `pip install -e source/isaaclab` 을 핀 없이 돌리면 리졸버가 cmeel-boost 를 시작으로
#   pinocchio(pin) / dex-retargeting / pytransform3d 서브트리를 끝없이 백트래킹한다.
#   8.3MB sdist 를 버전마다 내려받으며 되짚어서 수십 분이 지나도 끝나지 않는다.
#     INFO: pip is still looking at multiple versions of cmeel-boost ...
#
# 이 값들은 추측이 아니다:
#   실행 중인 isaac-lab-base 컨테이너에서 같은 명령이 **성공적으로 해결한** 조합을
#   그대로 `pip freeze` 로 뽑은 것이다. 검증된 조합을 고정해 탐색 공간을 없앤다.
#
# 재생성 방법 (조합을 다시 확인해야 할 때):
#   docker exec isaac-lab-base /isaac-sim/python.sh -m pip freeze
#
# isaaclab 자신은 editable 설치 대상이므로 여기 넣지 않는다.

annotated-doc==0.0.5
anytree==2.13.0
cmeel==0.60.1
cmeel-assimp==5.4.3.1
cmeel-boost==1.83.0
cmeel-console-bridge==1.0.2.3
cmeel-octomap==1.10.0
cmeel-qhull==8.0.2.1
cmeel-tinyxml==2.6.2.3
cmeel-urdfdom==3.1.1.1
cmeel-zlib==1.3.2
daqp==0.7.2
dex_retargeting==0.4.6
eigenpy==3.5.1
einops==0.8.2
flaky==3.8.1
gymnasium==1.2.1
hidapi==0.14.0.post2
hpp-fcl==2.4.4
iniconfig==2.3.0
junitparser==5.0.1
loop-rate-limiters==1.2.0
nlopt==2.7.1
pin==2.7.0
pin-pink==3.1.0
pluggy==1.6.0
prettytable==3.3.0
pyglet==1.5.31
pytest==9.1.1
pytest-mock==3.15.1
pytransform3d==3.16.0
qpsolvers==4.13.0
quadprog==0.1.13
regex==2026.7.19
safetensors==0.8.0
shellingham==1.5.4
starlette==0.49.1
tokenizers==0.22.2
transformers==5.14.1
typer==0.27.0
warp-lang==1.16.0
